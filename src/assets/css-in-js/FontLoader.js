import { createGlobalStyle } from 'styled-components';

export const FontLoaderStylesheet = createGlobalStyle`
  @font-face {
      font-family: 'Trade Gothic';
      font-weight: normal;
      font-style: normal;
  }

  @font-face {
      font-family: 'Circular Bold';
      font-weight: normal;
      font-style: normal;
  }

  @font-face {
      font-family: 'Circular Book';
      font-weight: normal;
      font-style: normal;
  }
`;
